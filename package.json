{
  "devDependencies": {
    "husky": "^1.3.1",
    "lint-staged": "^8.1.5",
    "prettier": "1.17.0"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "prettier --write",
      "git add"
    ]
  },
  "private": true,
  "scripts": {
    "dist": "yarn --cwd ./packages/ts-action dist && yarn --cwd ./packages/ts-action-operators dist",
    "test": "yarn --cwd ./packages/ts-action test && yarn --cwd ./packages/ts-action-operators test"
  },
  "workspaces": [
    "packages/*"
  ]
}
